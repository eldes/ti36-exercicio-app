import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  home: {
    flexGrow: 1,
    backgroundColor: '#eee',
    padding: 16,
  },

  button: {
    marginVertical: 8,
    backgroundColor: '#fff',
    color: '#999',
    padding: 8,
    borderRadius: 4,
  },
});

export default styles;