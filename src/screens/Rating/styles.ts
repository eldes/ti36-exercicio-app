import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  screen: {
    flexGrow: 1,
  },

  photos: {
    flexGrow: 1,
  },

  photo: {
    margin: 16,
    alignSelf: 'center',
  },

  photoImage: {
    width: 200,
    height: 200,
  },
});

export default styles;